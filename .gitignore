# --- Personal / health data — NEVER commit ---
# Real memory files hold private health, training, and physiological data.
# Only the *.example.md templates are tracked.
memory/*.md
!memory/*.example.md

# Personal project-tracking doc (dated training progress lives here)
Status.md

# Secrets / local config
.env
.env.*
.claude/settings.local.json

# OS / editor junk
Thumbs.db
Desktop.ini
.DS_Store
*.swp
*~
